import 'dart:ui';

class CometTheme {
  static const tabbarHeight = 60.0;
  static const dividerWidth = 2.0;
  static const footerHeight = 200.0;
  static const colorNavy = Color.fromARGB(255, 43, 47, 90);
}
